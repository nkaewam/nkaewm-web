## Project Guidelines

- Always use `bun` as runtime and package manager, _NEVER_ use npm, pnpm, yarn, or equivalent
- Always try to use commands from `package.json` before digging into any binaries under `node_modules/`. If commands are not available, please ask before trying to run binaries directly.
